/* Peak freestanding types for Karamel — uniquely named so krml/internal/types.h
 * does not #include itself via "types.h" (same-directory search). */
#ifndef PEAK_KRML_TYPES_H
#define PEAK_KRML_TYPES_H
#include "types.h"
#endif
