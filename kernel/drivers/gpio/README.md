# GPIO drivers (placeholder)

Portable GPIO leaf drivers will live here.

Raspberry Pi GPIO currently lives under `kernel/platform/rpi/gpio.c`.
Do not add board-specific code here until a portable `gpio` HAL seam is ready.
